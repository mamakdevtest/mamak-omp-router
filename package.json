{
  "name": "mamak-omp-router",
  "private": true,
  "version": "0.1.0",
  "description": "Marketplace workspace for the Mamak Router oh-my-pi plugin.",
  "workspaces": [
    "plugins/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun run --filter '*' test"
  },
  "engines": {
    "bun": ">=1.3.14"
  }
}
